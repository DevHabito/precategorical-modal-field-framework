# Changelog — A81

- Added the exact two-variable reduction of the gamma-inactive compressed branch.
- Derived closed parity formulas for `P_r`, `Q_r`, the active 2x2 system, and both gamma boundary polynomials.
- Derived all six boundary coefficients explicitly.
- Reconstructed all 142 committed A80 selected-contact Cramer polynomials exactly.
- Certified positive `T` and `Delta` on the local interval for all 1,438 admissible `(M,k)` pairs.
- Proved the raw boundary-gap identity `F_lower-F_upper=4 epsilon T`.
- Reproduced the A80 root atlas from endpoint signs alone and excluded lower-only/reversed selected pairs.
- Recorded primitive-content asymmetries at `M=34,64,69,77` instead of forcing a false common normalization.
- Added A81 tests, registry/index entries, runbook, reproducibility contract, and release metadata updates.
