# A114-B2-C3 — QI/QA residual branch theorem on the strict `b+2`, negative-pivot tail

## Status

**PROVED conditional branch theorem under the frozen analytic-tail contract, subject to the executable certificate and logical audit packaged with this file.**

Let

\[
M\ge521,\qquad \frac{129}{1000}\le s\le\frac{133}{1000},
\qquad h=\lfloor M/2\rfloor,
\]

\[
b=\left\lceil\frac{M\log2}{-2\log s}\right\rceil,
\qquad j=b+2,
\qquad U=2^{-h}.
\]

Assume `j=b+2` is the strict compressed maximizer and

\[
\Phi=F_j^{\rm up}<0.
\]

Use the already-promoted residual architectures

\[
C:\ P=\{0,j,M\},\quad Q=\{1,h,h+1\},
\]

\[
E:\ P=\{j-1,j,M\},\quad Q=\{1,h,h+1\},
\]

both with `alpha+`, `beta-` active and gamma inactive, and define

\[
QI:\ P=\{j,M\},\quad Q=\{0,1,h,h+1\},
\]

with `alpha+`, `beta-` active and gamma inactive, and

\[
QA:\ P=\{j-1,j,M\},\quad Q=\{0,1,h,h+1\},
\]

with `alpha+`, `beta-`, `gamma-` active.

Assume the residual interior premises

\[
\boxed{p_0^C<0,\qquad r_E(q_0)<0.}
\]

Finally define the last residual discriminant

\[
\boxed{\Gamma:=S_{\gamma-}^{QI}}.
\]

## Theorem

Under the contract and strict residual premises above:

\[
\boxed{\Gamma>0\Longrightarrow QI}
\]

and

\[
\boxed{\Gamma<0\Longrightarrow QA}.
\]

In each branch the selected basis satisfies the complete strict KKT system of the declared finite lifted LP and is therefore the unique strict global basic optimum.

The set `Gamma=0` is a non-strict QI/QA transition and is **not** included in the strict theorem.

Thus, together with A114-B2-C1 and A114-B2-C2, the strict interior of the `b+2`, `Phi<0` tail is classified by

\[
\boxed{
\begin{array}{rcl}
p_0^C>0 &\Longrightarrow& C,\\
p_0^C<0,\ r_E(q_0)>0 &\Longrightarrow& E,\\
p_0^C<0,\ r_E(q_0)<0,\ \Gamma>0 &\Longrightarrow& QI,\\
p_0^C<0,\ r_E(q_0)<0,\ \Gamma<0 &\Longrightarrow& QA.
\end{array}}
\]

Zero-discriminant sets remain separate: `p0^C=0`, `r_E(q0)=0`, `Gamma=0`, and `Phi=0` are not silently absorbed into a strict branch.

---

## Proof map

### 1. Frozen source/tail consequences

A114-B2-B and the A114-B2-C source-box certificate already give

\[
\frac9{1000}<Q:=\frac{s^{j-1}}U<\frac1{25},
\]

\[
3j-h\ge13,\qquad \frac{\beta^j}{U}\le2^{-13},
\]

and the contact localization gives

\[
h-j\ge168,\qquad M-j>400.
\]

The C3 analytic certificate rechecks the rational consequences actually used below and gives explicit remainder caps for all discarded remote powers. No floating-point sign decision is used in the certificate.

### 2. QI reduced system and determinant orientation

After eliminating the normalization/mean blocks, write the Q interpolation response for a transform base `f` as

\[
Q_f=C_f+D_ft-L_fq_0.
\]

The active source/beta rows of `QI` reduce to a two-by-two system whose determinant is

\[
\boxed{D_Q=L_\beta K_s-L_sK_\beta}.
\]

The certificate proves uniformly, for both parities,

\[
D_Q>0.
\]

The current compressed basis and QI have the same exact Cramer numerator

\[
N=C_sK_\beta-C_\beta K_s.
\]

A114-B2-C2 has the opposite compressed determinant orientation. Hence

\[
\boxed{p_0^C<0\Longrightarrow q_0^{QI}>0}.
\]

For the endpoint basis write

\[
D_E=A_sK_\beta-A_\beta K_s.
\]

The C3 certificate proves `D_E>0`. The exact bordered identities are

\[
r_E(q_0)=\frac{N_g}{D_E},
\qquad
r_{QI}(p_{j-1})=-\frac{N_g}{D_Q}.
\]

Therefore the residual premise `r_E(q0)<0` gives

\[
\boxed{r_{QI}(p_{j-1})>0}.
\]

### 3. QI primal, dual, slacks and Q reduced costs

The reduced QI scale variable satisfies a positive Cramer numerator over `D_Q>0`; the certificate gives uniform positive lower bounds on `Ut`. The exact P mean formulas then give

\[
t>0,\qquad p_j^{QI}>0,\qquad p_M^{QI}>0.
\]

Together with `q0^QI>0`, the beta/gamma subtraction and the hypothesis `Gamma>0` give

\[
q_1^{QI}>0.
\]

The exact central-Q formulas, the fixed `Ut` lower bound, and the exponentially small `U` terms then give

\[
q_h^{QI}>0,\qquad q_{h+1}^{QI}>0.
\]

The two active multipliers have positive closed numerators. The opposite alpha/beta slacks equal `4 epsilon t>0`, while a separate uniform threshold proves the inactive gamma-plus slack positive. The branch hypothesis itself is the gamma-minus slack `Gamma>0`.

For Q reduced costs, interpolate `s^x` at the four basic Q nodes

\[
0,1,h,h+1
\]

by the span generated by `1,x,tau^x,beta^x`. Let `R_s(x)` be the interpolation remainder. The established generalized-Vandermonde orientation gives

\[
R_s(x)<0
\]

at every nonbasic integer. The QI reduced cost is exactly

\[
r_{QI}(q_x)=-y_\alpha^{QI}R_s(x),
\]

so every nonbasic Q reduced cost is strictly positive.

### 4. QI P reduced costs

The QI P reduced-cost function lies in the already validated five-dimensional extended-Chebyshev space

\[
\operatorname{span}\{1,x,s^x,\beta^x,\tau^x\}.
\]

It has basic zeros at `j,M`. The pivot above supplies

\[
r_{QI}(p_{j-1})>0,
\]

while the analytic certificate gives the positive sentinel

\[
r_{QI}(p_{j+1})>0.
\]

It also proves `r_QI(p_0)>0` using the positive dual-gap margin amplified by

\[
2^{h-j}\ge2^{168}.
\]

As `x->-infinity`, the negative beta exponential dominates, so the reduced-cost function has an additional real zero to the left of zero. The five-dimensional ECT zero budget, together with the roots at `j,M` and the positive sentinels, leaves no room for an additional sign-changing excursion at a nonbasic integer. Hence

\[
\boxed{r_{QI}(p_x)>0\quad\forall x\notin\{j,M\}}.
\]

This completes strict KKT for the `Gamma>0` QI branch.

### 5. QA pivot orientations

The QA reduced three-by-three determinant is denoted `D_QA`. The analytic certificate proves, uniformly,

\[
\boxed{D_{QA}>0}.
\]

The two exact bordered identities are

\[
\boxed{
y_{\gamma-}^{QA}
=-\,r_E(q_0)\frac{D_E}{D_{QA}}}
\]

and

\[
\boxed{
U p_{j-1}^{QA}
=-\Gamma\frac{D_Q}{D_{QA}}}.
\]

Thus the residual premise and `Gamma<0` imply

\[
y_{\gamma-}^{QA}>0,
\qquad
p_{j-1}^{QA}>0.
\]

The same edge geometry gives `q0^QA>0`: the endpoint E basis has positive gamma-minus slack, QI has negative gamma-minus slack on the QA side, and the gamma-zero cut lies strictly between the exchanged coordinates `p_(j-1)` and `q0`.

**This edge argument is used only for `p_(j-1)^QA`/`q0^QA`. It is not used to infer positivity of the common QI/QA masses.**

### 6. Independent QA primal protection

The common QA masses are proved directly. Put

\[
w=U p_{j-1}^{QA},\qquad x=Ut^{QA}.
\]

The Cramer numerator for `x` has a certified positive lower bound. The ratio satisfies

\[
\frac wx<\frac14
\]

uniformly; the hard bounds are approximately `0.222` in even parity and `0.144` in odd parity. Consequently the exact P formulas give

\[
t^{QA}>0,\qquad p_j^{QA}>0,\qquad p_M^{QA}>0.
\]

The beta-minus and gamma-minus equalities, subtracted exactly, give a direct positive lower bound for `q1^QA`. The source/beta leading term dominates the central tails by the already-proved separation `h-j>=168`; no feasibility of the QI endpoint is assumed. Therefore

\[
q_1^{QA}>0.
\]

The exact central-Q block and the positive lower bound on `Ut` then give

\[
q_h^{QA}>0,\qquad q_{h+1}^{QA}>0.
\]

Thus every QA basic mass is strictly positive by an argument independent of QI primal feasibility.

### 7. QA duals and Q reduced costs

The certificate proves the alpha and beta active dual numerators positive, while the bordered identity above gives `y_(gamma-)>0`. It also proves the useful strict gap

\[
\boxed{y_\alpha^{QA}-y_\beta^{QA}-y_\gamma^{QA}>0}.
\]

For Q atoms use the same four basic interpolation nodes. If `R_s` and `R_gamma` denote the source and gamma remainders relative to the span `{1,x,tau^x,beta^x}`, generalized-Vandermonde orientation gives

\[
R_s(x)<0<R_\gamma(x)
\]

for every nonbasic integer because

\[
\gamma<\beta<s<\tau.
\]

The exact reduced-cost identity is

\[
r_{QA}(q_x)
=-y_\alpha^{QA}R_s(x)+y_\gamma^{QA}R_\gamma(x)>0.
\]

Hence every nonbasic Q reduced cost is strictly positive.

### 8. QA P reduced costs

The QA P reduced-cost function lies in the six-dimensional extended-Chebyshev space

\[
\operatorname{span}\{1,x,s^x,\beta^x,\gamma^x,\tau^x\}.
\]

It has basic roots at

\[
j-1,\quad j,\quad M.
\]

The analytic certificate proves the positive sentinels

\[
r_{QA}(p_0)>0,
\qquad
r_{QA}(p_{j-2})>0,
\qquad
r_{QA}(p_{j+1})>0,
\qquad
r_{QA}(p_{M-1})>0.
\]

The reported normalized margins at the two contact-neighbor sentinels remain comfortably positive in both parities. As `x->-infinity`, the negative gamma exponential dominates, producing a left external root. On the far right the certified affine-tail coefficient has the orientation required by the root at `M`; the remote exponential terms are separately bounded. These facts exhaust the five-zero budget of the six-dimensional ECT family. Therefore no additional sign-changing excursion can occur at a nonbasic P atom, and

\[
\boxed{r_{QA}(p_x)>0\quad\forall x\notin\{j-1,j,M\}}.
\]

The opposite slacks of alpha+, beta-, gamma- are all `4 epsilon t>0`. Hence QA satisfies complete strict KKT.

---

## Executable certification and falsification controls

`a114b2c3_qi_qa_analytic_certificate.py` proves **97/97** rational gates. In particular it certifies positive orientations for `D_Q,D_E,D_QA`, the QA direct-primal bounds, active-dual margins, Q interpolation orientations used in the composition, and the P sentinels/ECT inputs.

`a114b2c3_fraction_crosscheck.py` is an independent `fractions.Fraction` reconstruction of the raw QI/QA bases. It reports **79/79** gates on the six adversarial QI/QA controls and independently checks the pivot signs, raw equations, selected primal/dual/slack gates and reduced-cost sentinels.

The older, independently written `A114B2C_12_POINT_FULL_KKT_SUMMARY_20260913.json` supplies complete atom-by-atom `2M+9` exact KKT scans for those same six QI/QA controls. These finite controls are regression/falsification checks only; they are not premises of the all-tail proof.

The new crosscheck also preserves a crucial negative result. At all three QA controls the rival QI basis already has

\[
q_1^{QI}<0.
\]

Therefore the tempting statement “all common QA masses are positive because QA lies between two primal-feasible E/QI endpoints” is false and is **not** used by this theorem. The common QA masses are protected by the independent Cramer/beta-gamma/Q-block argument in Section 6.

## Dependencies and claim boundary

The proof imports only previously promoted structural results within their declared scope:

- A114-B2-B and the A114-B2-C source box;
- A114-B2-C1 for the endpoint branch protections and residual coordinate `r_E(q0)`;
- A114-B2-C2 for the compressed determinant orientation and `p0^C` branch boundary;
- the previously validated generalized-Vandermonde and extended-Chebyshev zero-count results for the displayed exponential families.

It does **not** depend on the refuted A108 one-sided conjecture, and it does not use the finite C/E/QI/QA census as an all-tail premise.

This theorem does **not** classify strict uniqueness on

\[
\Phi=0,\qquad p_0^C=0,\qquad r_E(q_0)=0,\qquad \Gamma=0,
\]

nor anything outside the frozen source/tail contract. No physical interpretation is claimed.
