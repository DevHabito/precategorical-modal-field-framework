# A114-B2 frontier update — 2026-09-13

A114-B2-B proves, under the frozen analytic-tail contract, that in the strict compressed `b+2` phase with `Phi=F_(b+2)^up<0`, the pure central-Q gamma-minus architecture

`P={0,j-1,j,M}`, `Q={1,h,h+1}`

cannot be strictly primal feasible and therefore cannot be a strict KKT optimum.

The remaining negative-pivot architectures observed exactly are:

- compressed two-band;
- endpoint-released;
- q0/q1 with gamma inactive;
- q0/q1 with gamma-minus active.

Their all-tail partition remains open. `Phi=0` also remains open.

The repository root A114 README was intentionally left unchanged in this publication because the connected GitHub write route rejected the full-size README replacement. This note records the frontier without silently truncating or rewriting the existing README.
