# A107-B2 — Prospective Gamma-Plus Batch Audit

## Frozen protocol

The preregistration `A107_B2_PREREGISTRATION.json` was frozen before execution with SHA-256:

`233d0f5424894f0cdf967e868f85e2d0c35b923ab8388d620ea78e246f1f5c91`

The selected sample was fixed as canonical `legacy_three_band_gamma_plus` ranks 10 through 17 inclusive under the A107 canonical ordering `(maximum, compressed_maximizer_contact, witness)`, with no replacement.

Three questions were preregistered:

1. Do all eight records possess a nonzero witness-containing open strict-KKT component under the frozen A102 architecture?
2. For every proper strict subcomponent, are all selected internal boundaries `basic_p_{j+1}`?
3. On `unique_b_plus_1` records only, does the finite prospective rule
   - `M mod 5 in {0,4}` -> `proper_strict_subcomponent`
   - `M mod 5 in {1,2}` -> `full_segment_coverage`
   hold for all in-scope B2 records?

No prediction was preregistered for residue 3 or for non-`unique_b_plus_1` phases.

## Results

Primary verdict: **PASS_BATCH_LOCAL_STABILITY**.

All eight selected records passed the exact local-stability criteria. The batch contained six `full_segment_coverage` records and two `proper_strict_subcomponent` records. Across the eight cases, 536 strict KKT conditions were reconstructed. Independent direct-matrix/rank-one checkpoint regression performed 1,736 exact rational comparisons, with zero mismatches.

| Rank | M | j | M mod 5 | Phase | Observed class | Selected boundary | Residue rule |
|---:|---:|---:|---:|---|---|---|---|
| 10 | 25 | 6 | 0 | `unique_b_plus_1` | proper strict subcomponent | `basic_p_7`, left | match |
| 11 | 26 | 6 | 1 | `unique_b_plus_1` | full segment coverage | none | match |
| 12 | 27 | 6 | 2 | `unique_b_plus_1` | full segment coverage | none | match |
| 13 | 28 | 6 | 3 | `b_plus_2_to_b_plus_1` | full segment coverage | none | out of scope |
| 14 | 30 | 7 | 0 | `unique_b_plus_1` | proper strict subcomponent | `basic_p_8`, left | match |
| 15 | 31 | 7 | 1 | `unique_b_plus_1` | full segment coverage | none | match |
| 16 | 32 | 7 | 2 | `unique_b_plus_1` | full segment coverage | none | match |
| 17 | 33 | 7 | 3 | `unique_b_plus_1` | full segment coverage | none | no preregistered classification |

The two partial cases therefore support the preregistered boundary mechanism: both exit the strict component on the left through the upper adjacent basic atom, `p_{j+1}=0`. The boundary verdict is **SUPPORT_BATCH**.

The finite residue prediction had six in-scope records and obtained six exact matches with zero mismatches. Its verdict is **PROSPECTIVE_SUPPORT_B2_FINITE_RULE**. This is prospective support for the finite B2 rule, not a theorem in `M`, not a theorem for all 922 gamma-plus records, and not evidence for residue 3 or non-`unique_b_plus_1` phases.

The two isolated left boundaries are approximately:

- rank 10 (`M=25`, `j=6`): `s_* ≈ 0.12953412050942248`, generated by `basic_p_7`;
- rank 14 (`M=30`, `j=7`): `s_* ≈ 0.1297961564791439`, generated by `basic_p_8`.

Exact rational root-isolation brackets and certificates are stored in the result JSON.

## Certification checks

For every record: core certificate failures = 0, nonselected-boundary hull certificate failures = 0, root-ordering failures = 0, root-certification failures = 0, witness failures = 0, unresolved cases = 0, and direct exact checkpoint mismatches = 0.

The audit was executed twice from the same frozen preregistration. The two result JSON files were byte-identical. The result SHA-256 is:

`d0507529e7805eb4c763769630482646e4261c2c9d2def7e2ee89b73e7c6f9d0`

## Scope

This audit establishes exact local-continuum behavior only for canonical gamma-plus ranks 10–17 under the A95/A102 source segments. It does not establish a universal gamma-plus theorem, an all-`M` law, a classification of residue 3, behavior outside the source segments, or any physical claim.
